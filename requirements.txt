# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp                     # Download audio from YouTube URLs
pydub                      # Audio file manipulation / format conversion
ffmpeg-python               # FFmpeg bindings (FFmpeg binary must be installed separately)

# ── Speech-to-Text ─────────────────────────────────────────────────────────────
requests                   # Used for Groq Whisper + Sarvam AI STT API calls

# ── LLM Orchestration — LangChain LCEL + Groq ─────────────────────────────────
langchain
langchain-core
langchain-community
langchain-groq             # LangChain wrapper for Groq API
groq

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
langchain-chroma            # LangChain ↔ Chroma vector store bridge
chromadb                    # Local vector store
langchain-google-genai
tiktoken                    # Token counting for text splitting

# ── Backend API ────────────────────────────────────────────────────────────────
fastapi>=0.138.0            # app.frontend() requires this version or newer
uvicorn[standard]           # ASGI server
python-multipart             # Required for file uploads (UploadFile / Form)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv               # Load API keys from .env
numpy
tqdm                        # Progress bars


reportlab